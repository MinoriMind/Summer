unit ThirdFileCreation;

interface
  uses GlobalTypes, FirstFileCheck, SecondFileCheck;

  procedure CreateFile (tow: TableOfWorkers; toc: TableOfCatalog);


implementation
procedure CreateFile (tow: TableOfWorkers; toc: TableOfCatalog);
begin
end;
//будет выводить массив в файл




procedure PutToArray(tow: TableOfWorkers; toc: TableOfCatalog);
var toq: TableOfReq;
begin
//если пришло время аттестаровиться: год аттестации + пероид > указанной даты
//которую кстати надо считывать!!!
end;
//будет формировать эррей от реквайрмент


begin

end.